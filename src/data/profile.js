// Single source of truth for all personal content on the site.
// This same file (shared/profile.json) is also read by the server
// so the AI agent answers from exactly the same data shown on the page.
import profile from '../../shared/profile.json'

export default profile
